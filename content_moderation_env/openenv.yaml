version: "1.0"
name: "content_moderation"
version: "1.0.0"
description: "OpenEnv Content Moderation Environment - Real-world content moderation with 3 graded tasks (easy spam → medium toxic → hard mixed)"
tags: ["openenv"]
entrypoint: "inference:app"
port: 8000
