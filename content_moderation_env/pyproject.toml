[project]
name = "content-moderation-env"
version = "0.1.0"
description = "OpenEnv content moderation environment"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic",
  "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"
